services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bypur_api
    ports:
      - "3001:3001"
    env_file:
      - .env
    networks:
      - bypur_network

networks:
  bypur_network:
    driver: bridge
